[package]
name = "slider_detect"
version = "0.1.0"
edition = "2021"

[lib]
name = "slider_detect"
crate-type = ["cdylib"]

[dependencies]
opencv = { version = "0.98.2", default-features = false, features = ["clang-runtime", "imgcodecs", "imgproc"] }

[profile.release]
opt-level = 3
